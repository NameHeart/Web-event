import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'freedon_innovation_tech_website',
  webDir: 'pages',
  server: {
    androidScheme: 'https'
  }
};

export default config;
